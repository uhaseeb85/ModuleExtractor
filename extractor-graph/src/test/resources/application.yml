# Test application properties for extractor-graph integration tests.
# Actual Neo4j connection is overridden at runtime by @DynamicPropertySource.
spring:
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: ''

logging:
  level:
    com.extractor: DEBUG
    org.springframework.data.neo4j: WARN
